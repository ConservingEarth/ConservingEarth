= simple_form_for @video, html: {multipart: true } do |f|
	- if @video.errors.any?
		#errors
		%h2
		= pluralize(@video.errors.count, "error")
		prevent this Video from saving
		%ul
			- @video.errors.full_messages.each do |msg|
				%li= msg

	.form-group
		= f.input :image, imput_html: {class: 'form-control'}

	.form-group
		= f.input :title, imput_html: {class: 'form-control'}

	.form-group
		= f.input :description, imput_html: {class: 'form-control'}
		
	 
	
	= f.button :submit, class: "btn btn-primary"