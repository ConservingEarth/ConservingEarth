= simple_form_for @video, html: {multipart: true }, validate: true do |f|
	- if @video.errors.any?
		#errors
		%h2
		= pluralize(@video.errors.count, "error")
		prevent this Video from saving
		%ul
			- @video.errors.full_messages.each do |msg|
				%li= msg

	.form-group
		Currently only youtube videos are supported.

	.form-group
		= f.input :link, imput_html: {class: 'form-control'}

	.form-group
		= f.input :description, imput_html: {class: 'form-control'}
		
	 
	
	= f.button :submit, class: "btn btn-primary"